name: whatsapp-njabuloelite
services:
  - name: whatsapp-njabuloelite-SERVICE
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
